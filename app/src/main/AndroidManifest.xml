<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/appass_launcher"
        android:label="Практикум"
        android:roundIcon="@mipmap/appass_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assembler"
        tools:targetApi="31">
        <activity
            android:name=".el_theory"
            android:exported="false" />
        <activity
            android:name=".TaskList"
            android:exported="true"/>
        <activity
            android:name=".TheoryActivity"
            android:exported="false" />
        <activity
            android:name=".TaskActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>